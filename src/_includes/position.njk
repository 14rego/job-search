<li>
    <h3 class="flex-cols">
        <strong>{{ job.title }};</strong> 
        <span>
            <strong>{{ job.company }};</strong>
            <span>{{ job.city }}, {{ job.state }}</span> 
        {%- if job.end == 'Current' %}
            <span>(<em>{{ job.start | yyyy }}-{{ job.end }}</em>)</span>
        {%- else %}
            <span>(<em>{{ job.start | yyyy }}-{{ job.end | yyyy }}</em>)</span>
        {%- endif %}
        </span>
    </h3>
    <ul>
        {%- for b in job.bullets %}<li>{{ b }}</li>{%- endfor %}
    </ul>
</li>
